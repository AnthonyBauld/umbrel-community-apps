version: "3.7"

services:
  app_proxy:
    environment:
      APP_HOST: rotki_rotki_1
      APP_PORT: 8080

  rotki:
    image: rotki/rotki:v1.36.0
    restart: on-failure
    volumes:
      - ${APP_DATA_DIR}/data:/root/.rotki
    environment:
      - ROTKI_DATA_DIR=/root/.rotki
