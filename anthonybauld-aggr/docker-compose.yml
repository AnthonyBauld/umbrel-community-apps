version: "3.7"

services:
  app_proxy:
    image: getumbrel/app-proxy:v1.3.0
    restart: on-failure
    environment:
      APP_HOST: anthonybauld-aggr_web_1
      APP_PORT: 80

  web:
    image: ghcr.io/anthonybauld/aggr:latest
    restart: on-failure
    environment:
      - PORT=80
