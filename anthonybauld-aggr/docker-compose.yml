services:
  app_proxy:
    image: getumbrel/app-proxy:v0.1.1
    restart: on-failure
    environment:
      APP_HOST: web
      APP_PORT: 80

  web:
    image: ghcr.io/anthonybauld/aggr:latest
    restart: on-failure
    environment:
      - PORT=80
